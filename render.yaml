services:
  - type: web
    name: cafe-website
    env: node
    plan: free
    autoDeploy: true
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    # No disk on Free plan; SQLite will be ephemeral (resets on redeploy/restart)


